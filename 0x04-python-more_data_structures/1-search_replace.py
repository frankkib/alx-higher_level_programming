#!/usr/bin/python3
def search_replace(my_list, search, replace):
    if not my_list:
        return my_list
    new_list = []
    for number, new_number in enumerate(new_list):
        if new_number == search:
            new_list[number] = replace
            return new_list
        else:
            return my_list
